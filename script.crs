some = v. (Some, v);
none = None;

some = v. (Some, v);
none = None;

map = f.o. o <> (Some, v) ? some (f v) : none;

rec = n. n == 0 ? 0 : n + (rec n - 1);

compose = f.s.a. f(s a);

mapRec = map rec;

mapPrint = map print;

c = compose mapPrint mapRec;

some 10 |> print;

b = (1, (Some, (5, None)));

(1, (Some, (v, None))) <> b;

